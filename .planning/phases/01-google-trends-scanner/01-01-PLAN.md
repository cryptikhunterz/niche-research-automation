---
phase: 01-google-trends-scanner
plan: 01
type: execute
---

<objective>
Build Google Trends scanner that identifies high-growth keywords for e-commerce niche research.

Purpose: Automate the discovery process that found magnesium glycinate (+550% growth)
Output: Working scanner script + CSV with keywords ranked by growth potential
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Seed Keywords:** 47 keywords across 6 categories (supplements, women's health, gut health, sleep/stress, natural beauty, men's optimization)

**Technical:** pytrends library (unofficial Google Trends API), Python 3.12
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scanner script with pytrends integration</name>
  <files>niche_scanner.py</files>
  <action>
    Build Python script that:
    1. Accepts seed keywords organized by category
    2. Fetches interest_over_time data via pytrends
    3. Calculates growth % across 5 horizons (5yr, 1yr, 6mo, 3mo, 1mo)
    4. Gets related_queries and rising_queries
    5. Calculates weighted recommendation_score
    6. Handles rate limits with delays (15s) and retries (30s backoff)
    7. Saves checkpoints every 10 keywords
    8. Outputs sorted CSV

    Scoring formula: score = (1mo * 0.3) + (3mo * 0.25) + (6mo * 0.2) + (1yr * 0.15) + (5yr * 0.1)
    Cap growth at 10000% to avoid infinity.
  </action>
  <verify>python3 niche_scanner.py runs without errors for test subset</verify>
  <done>Script exists, handles rate limits, produces valid output</done>
</task>

<task type="auto">
  <name>Task 2: Run full keyword scan</name>
  <files>niche_trends_results.csv</files>
  <action>
    Execute scanner against all 47 seed keywords.
    Expected runtime: 15-20 minutes (47 keywords × 15s delay + retries)
    Save results to niche_trends_results.csv
  </action>
  <verify>CSV exists with 40+ rows (allowing for some failures)</verify>
  <done>Full scan complete, results sorted by recommendation_score</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Google Trends scanner with growth metrics</what-built>
  <how-to-verify>
    1. Open niche_trends_results.csv
    2. Verify magnesium glycinate shows ~1200% 5yr growth (known baseline)
    3. Check top 10 keywords - do growth numbers make sense?
    4. Confirm recommendation_score ranking prioritizes recent growth
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] niche_scanner.py runs without errors
- [ ] niche_trends_results.csv contains 40+ keywords
- [ ] Magnesium glycinate shows expected growth (~1200% 5yr)
- [ ] Results sorted by recommendation_score descending
</verification>

<success_criteria>
- Scanner script complete and working
- Full scan executed
- CSV output ready for Phase 2 (keyword expansion) or Phase 3 (transcript mining)
</success_criteria>

<output>
After completion, create `.planning/phases/01-google-trends-scanner/01-01-SUMMARY.md`
</output>
